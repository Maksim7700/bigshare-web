import axios from 'axios';

export const checkUser = () => {
    // return axios({
    //     method: 'POST',
    //     url: `${process.env.hostUrl || 'http://localhost:8081'}/api/user/count`,
    // })
}
